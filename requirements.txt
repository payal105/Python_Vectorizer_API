# --- Web framework -----------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27

# --- Vectorization engine ----------------------------------------------------
# VTracer: Rust colour-clustering + curve-fitting tracer (ships native wheels)
vtracer>=0.6.11

# --- Raster handling ---------------------------------------------------------
pillow>=11.0

# --- Gradient refinement -----------------------------------------------------
# The second stage compares the finished vector against the source bitmap and
# fits real gradients to the shapes that shade. numpy carries the pixels and
# the least-squares fits; scipy supplies the labelled-region work (bounding
# boxes, erosion, per-bin medians) and the Nelder-Mead search for a radial
# centre; scikit-image supplies CIELAB, so that every threshold about colour
# means the same thing in dark colours as in light ones.
numpy>=2.0
scipy>=1.14
scikit-image>=0.24

# --- Vector output (SVG -> PDF / EPS / PNG) ----------------------------------
svglib>=1.5.1
reportlab>=4.2
lxml>=5.0
rlPyCairo>=0.3.0
